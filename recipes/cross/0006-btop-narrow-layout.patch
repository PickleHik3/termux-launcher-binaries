--- a/src/btop_config.cpp
+++ b/src/btop_config.cpp
@@ -251,10 +251,22 @@ namespace Config {
 
 	std::unordered_map<std::string_view, string> strings = {
 		{"color_theme", "Default"},
+	#ifdef __ANDROID__
+		//? A phone terminal is about 63 columns by 28 rows. That is too narrow for mem and net beside
+		//? proc, so the boxes stack, and 28 rows hold cpu, mem and a useful proc list but not all
+		//? four (29 rows). All four are preset 0 for when the terminal is zoomed out; "p" steps past
+		//? presets that do not fit. A config file that names shown_boxes or presets wins over these.
+		{"shown_boxes", "cpu mem proc"},
+	#else
 		{"shown_boxes", "cpu mem net proc"},
+	#endif
 		{"graph_symbol", "braille"},
 		{"disable_presets", "Off"},
+	#ifdef __ANDROID__
+		{"presets", "cpu:0:default,mem:0:default,proc:0:default cpu:0:default,proc:0:default proc:0:default cpu:0:default,mem:0:default,net:0:default"},
+	#else
 		{"presets", "cpu:1:default,proc:0:default cpu:0:default,mem:0:default,net:0:default cpu:0:block,net:0:tty"},
+	#endif
 		{"graph_symbol_cpu", "default"},
 		{"graph_symbol_gpu", "default"},
 		{"graph_symbol_mem", "default"},
--- a/src/btop_draw.cpp
+++ b/src/btop_draw.cpp
@@ -305,8 +305,11 @@ namespace Draw {
 
 	bool update_clock(bool force) {
 		const auto& clock_format = Config::getS("clock_format");
-		if (not Cpu::shown or clock_format.empty()) {
-			if (clock_format.empty() and not Global::clock.empty()) Global::clock.clear();
+		//? Between the preset button and the update button a box narrower than the wide-layout
+		//? minimum has no room for a clock
+		const bool clock_fits = Cpu::width >= Cpu::min_width;
+		if (not Cpu::shown or clock_format.empty() or not clock_fits) {
+			if ((clock_format.empty() or not clock_fits) and not Global::clock.empty()) Global::clock.clear();
 			return false;
 		}
 
@@ -518,6 +521,9 @@ namespace Draw {
 namespace Cpu {
 	int width_p = 100, height_p = 32;
 	int min_width = 60, min_height = 8;
+	//? Stacked: 44 columns keep two core columns beside the graph even with temperatures shown,
+	//? and leave the title row its menu, preset and update buttons
+	int narrow_min_width = 44, narrow_min_height = 8;
 	int x = 1, y = 1, width = 20, height;
 	int b_columns, b_column_size;
 	int b_x, b_y, b_width, b_height;
@@ -606,8 +612,9 @@ namespace Cpu {
 			Input::mouse_mappings["-"] = {button_y, x + width - (int)update.size() - 7, 1, 2};
 			Input::mouse_mappings["+"] = {button_y, x + width - 5, 1, 2};
 
-			// Draw container engine name
-			if (Cpu::container_engine.has_value()) {
+			// Draw container engine name (in a box narrower than the wide-layout minimum it would run
+			// into the update button, like the clock and the battery below)
+			if (Cpu::container_engine.has_value() and width >= min_width) {
 				fmt::format_to(std::back_inserter(out), "{}{}{}{}{}", Mv::to(button_y, x + 28), title_left, Theme::c("title"), Cpu::container_engine.value(), title_right);
 			}
 
@@ -731,8 +738,8 @@ namespace Cpu {
 			}
 		}
 
-		//? Draw battery if enabled and present
-		if (Config::getB("show_battery") and has_battery) {
+		//? Draw battery if enabled and present, and the title row has room for it
+		if (Config::getB("show_battery") and has_battery and width >= min_width) {
 			static int old_percent{};   // defaults to = 0
 			static long old_seconds{};  // defaults to = 0
 			static float old_watts{};	// defaults to = 0
@@ -770,7 +777,8 @@ namespace Cpu {
 			}
 		}
 		else if (bat_pos > 0) {
-			out += Mv::to(y, bat_pos) + Fx::ub + Theme::c("cpu_box") + Symbols::h_line * (bat_len + 4);
+			//? A redraw has already repainted the title row; the old position may lie past a narrower box
+			if (not redraw) out += Mv::to(y, bat_pos) + Fx::ub + Theme::c("cpu_box") + Symbols::h_line * (bat_len + 4);
 			bat_pos = bat_len = 0;
 		}
 
@@ -1191,6 +1199,8 @@ namespace Gpu {
 namespace Mem {
 	int width_p = 45, height_p = 40;
 	int min_width = 36, min_height = 10;
+	//? Stacked: 8 rows fit the total and the four memory lines, with disks beside them at full width
+	int narrow_min_width = 36, narrow_min_height = 8;
 	int x = 1, y, width = 20, height;
 	int mem_width, disks_width, divider, item_height, mem_size, mem_meter, graph_height, disk_meter;
 	int disks_io_h = 0;
@@ -1457,6 +1467,8 @@ namespace Mem {
 namespace Net {
 	int width_p = 45, height_p = 28;
 	int min_width = 36, min_height = 6;
+	//? Stacked: 6 is the least the inner stats box draws in without its rows landing on its border
+	int narrow_min_width = 36, narrow_min_height = 6;
 	int x = 1, y, width = 20, height;
 	int b_x, b_y, b_width, b_height, d_graph_height, u_graph_height;
 	bool shown = true, redraw = true;
@@ -1572,6 +1584,8 @@ namespace Net {
 namespace Proc {
 	int width_p = 55, height_p = 68;
 	int min_width = 44, min_height = 16;
+	//? Stacked: 7 rows show four processes; fewer and the scrollbar arithmetic divides by zero
+	int narrow_min_width = 44, narrow_min_height = 7;
 	int x, y, width = 20, height;
 	int start, selected, select_max;
 	bool shown = true, redraw = true;
@@ -2239,6 +2253,48 @@ namespace Draw {
 		Net::shown = boxes.contains("net");
 		Proc::shown = boxes.contains("proc");
 
+		//* Stacked layout for terminals too narrow to put mem/net beside proc: every shown box spans
+		//* the full width, top to bottom cpu, mem, net, proc (cpu_bottom and mem_below_net still
+		//* reorder). cpu, mem and net start at their stacked minimums; proc takes the first spare
+		//* rows up to 12, then the three grow a little, and proc keeps whatever is left.
+		const bool narrow = Term::narrow_layout(boxes);
+		int n_cpu_h = 0, n_mem_h = 0, n_net_h = 0, n_proc_h = 0;
+		int n_cpu_y = 1, n_mem_y = 1, n_net_y = 1, n_proc_y = 1;
+		if (narrow) {
+			if (Cpu::shown) n_cpu_h = Cpu::narrow_min_height;
+			if (Mem::shown) n_mem_h = Mem::narrow_min_height;
+			if (Net::shown) n_net_h = Net::narrow_min_height;
+			if (Proc::shown) n_proc_h = Proc::narrow_min_height;
+			int spare = max(0, Term::height - (n_cpu_h + n_mem_h + n_net_h + n_proc_h));
+			auto grow = [&spare](int& h, const int cap) {
+				if (h == 0) return;
+				const int add = clamp(cap - h, 0, spare);
+				h += add;
+				spare -= add;
+			};
+			grow(n_proc_h, 12);
+			grow(n_cpu_h, 12);
+			grow(n_mem_h, 12);
+			grow(n_net_h, 10);
+			if (Proc::shown) n_proc_h += spare;
+			else if (Net::shown) n_net_h += spare;
+			else if (Mem::shown) n_mem_h += spare;
+			else n_cpu_h += spare;
+
+			int y_next = 1;
+			if (Cpu::shown and not cpu_bottom) { n_cpu_y = y_next; y_next += n_cpu_h; }
+			if (mem_below_net) {
+				n_net_y = y_next; y_next += n_net_h;
+				n_mem_y = y_next; y_next += n_mem_h;
+			}
+			else {
+				n_mem_y = y_next; y_next += n_mem_h;
+				n_net_y = y_next; y_next += n_net_h;
+			}
+			n_proc_y = y_next; y_next += n_proc_h;
+			if (Cpu::shown and cpu_bottom) n_cpu_y = y_next;
+		}
+
 		//* Calculate and draw cpu box outlines
 		if (Cpu::shown) {
 			using namespace Cpu;
@@ -2250,6 +2306,13 @@ namespace Draw {
 				: 0;
 		#endif
             const bool show_temp = (Config::getB("check_temp") and got_sensors);
+			if (narrow) {
+				width = Term::width;
+				height = n_cpu_h;
+				x = 1;
+				y = n_cpu_y;
+			}
+			else {
 			width = round((double)Term::width * width_p / 100);
 		#ifdef GPU_SUPPORT
 			if (Gpu::shown != 0 and not (Mem::shown or Net::shown or Proc::shown)) {
@@ -2263,6 +2326,7 @@ namespace Draw {
 		#endif
 			x = 1;
 			y = cpu_bottom ? Term::height - height + 1 : 1;
+			}
 
 		#ifdef GPU_SUPPORT
 			b_columns = max(2, (int)ceil((double)(Shared::coreCount + 1) / (height - gpus_extra_height - 5)));
@@ -2370,6 +2434,13 @@ namespace Draw {
 			auto swap_disk = Config::getB("swap_disk");
 			auto mem_graphs = Config::getB("mem_graphs");
 
+			if (narrow) {
+				width = Term::width;
+				height = n_mem_h;
+				x = 1;
+				y = n_mem_y;
+			}
+			else {
 			width = round((double)Term::width * (Proc::shown ? width_p : 100) / 100);
 		#ifdef GPU_SUPPORT
 			height = floor(static_cast<double>(Term::height) * (100 - Net::height_p * Net::shown*4 / ((Gpu::shown != 0 and Cpu::shown) + 4)) / 100) - Cpu::height - Gpu::total_height;
@@ -2387,6 +2458,7 @@ namespace Draw {
 			else
 				y = cpu_bottom ? 1 : Cpu::height + 1;
 		#endif
+			}
 
 			if (show_disks) {
 				mem_width = ceil((double)(width - 3) / 2);
@@ -2434,6 +2506,13 @@ namespace Draw {
 		//* Calculate and draw net box outlines
 		if (Net::shown) {
 			using namespace Net;
+			if (narrow) {
+				width = Term::width;
+				height = n_net_h;
+				x = 1;
+				y = n_net_y;
+			}
+			else {
 			width = round((double)Term::width * (Proc::shown ? width_p : 100) / 100);
 		#ifdef GPU_SUPPORT
 			height = Term::height - Cpu::height - Gpu::total_height - Mem::height;
@@ -2449,6 +2528,7 @@ namespace Draw {
 			#endif
 			else
 				y = Term::height - height + 1 - (cpu_bottom ? Cpu::height : 0);
+			}
 
 			b_width = (width > 45) ? 27 : 19;
 			b_height = (height > 10) ? 9 : height - 2;
@@ -2468,6 +2548,13 @@ namespace Draw {
 		//* Calculate and draw proc box outlines
 		if (Proc::shown) {
 			using namespace Proc;
+			if (narrow) {
+				width = Term::width;
+				height = n_proc_h;
+				x = 1;
+				y = n_proc_y;
+			}
+			else {
 			width = Term::width - (Mem::shown ? Mem::width : (Net::shown ? Net::width : 0));
 		#ifdef GPU_SUPPORT
 			height = Term::height - Cpu::height - Gpu::total_height;
@@ -2480,6 +2567,14 @@ namespace Draw {
 		#else
 			y = (cpu_bottom and Cpu::shown) ? 1 : Cpu::height + 1;
 		#endif
+			}
+			//? The detail pane takes 8 rows off the list; a box shorter than the wide-layout minimum
+			//? has no room for it, so drop it here when a resize or a preset shrank the box
+			if (height < min_height and Config::getB("show_detailed")) {
+				Config::set("show_detailed", false);
+				Config::set("detailed_pid", 0);
+				Config::set("proc_last_selected", 0);
+			}
 			select_max = height - 3;
 			box = createBox(x, y, width, height, Theme::c("proc_box"), true, "proc", "", 4);
 		}
--- a/src/btop_input.cpp
+++ b/src/btop_input.cpp
@@ -272,7 +272,19 @@ namespace Input {
 					else Config::current_preset = (key == "p") ? first_preset : Config::preset_list.size() - 1;
 					if (Config::current_preset == old_preset) return;
 					atomic_wait(Runner::active);
-					if (not Config::apply_preset(Config::preset_list.at(Config::current_preset.value()))) {
+					//? Step past presets the terminal cannot hold (a phone at its normal zoom holds
+					//? three boxes, not four); the size error is for when none of them fits
+					bool applied = false;
+					const size_t tries = (Config::getS("disable_presets") == "Custom") ? 1 : Config::preset_list.size();
+					for (size_t i = 0; i < tries; i++) {
+						applied = Config::apply_preset(Config::preset_list.at(Config::current_preset.value()));
+						if (applied) break;
+						if (key == "p") {
+							if (++(*Config::current_preset) >= static_cast<int>(Config::preset_list.size())) Config::current_preset = first_preset;
+						}
+						else if (--(*Config::current_preset) < first_preset) Config::current_preset = Config::preset_list.size() - 1;
+					}
+					if (not applied) {
 						Menu::show(Menu::Menus::SizeError);
 						Config::current_preset = old_preset;
 						return;
@@ -461,6 +473,10 @@ namespace Input {
 					if (Config::getI("proc_selected") == 0 and not Config::getB("show_detailed")) {
 						return;
 					}
+					//? The detail pane needs a box at least the wide-layout minimum tall (see calcSizes)
+					else if (not Config::getB("show_detailed") and Proc::height < Proc::min_height) {
+						return;
+					}
 					else if (Config::getI("proc_selected") > 0 and Config::getI("detailed_pid") != Config::getI("selected_pid")) {
 						Config::set("detailed_pid", Config::getI("selected_pid"));
 						Config::set("proc_last_selected", Config::getI("proc_selected"));
--- a/src/btop_shared.hpp
+++ b/src/btop_shared.hpp
@@ -201,6 +201,7 @@ namespace Gpu {
 namespace Cpu {
 	extern string box;
 	extern int x, y, width, height, min_width, min_height;
+	extern int narrow_min_width, narrow_min_height;   //? per-box minimums for the stacked layout
 	extern bool shown, redraw, got_sensors, cpu_temp_only, has_battery, supports_watts;
 	extern string cpuName, cpuHz;
 	extern vector<string> available_fields;
@@ -258,6 +259,7 @@ namespace Cpu {
 namespace Mem {
 	extern string box;
 	extern int x, y, width, height, min_width, min_height;
+	extern int narrow_min_width, narrow_min_height;   //? per-box minimums for the stacked layout
 	extern bool has_swap, shown, redraw;
 	const array mem_names { "used"s, "available"s, "cached"s, "free"s };
 	const array swap_names { "swap_used"s, "swap_free"s };
@@ -305,6 +307,7 @@ namespace Mem {
 namespace Net {
 	extern string box;
 	extern int x, y, width, height, min_width, min_height;
+	extern int narrow_min_width, narrow_min_height;   //? per-box minimums for the stacked layout
 	extern bool shown, redraw;
 	extern string selected_iface;
 	extern vector<string> interfaces;
@@ -357,6 +360,7 @@ namespace Proc {
 
 	extern string box;
 	extern int x, y, width, height, min_width, min_height;
+	extern int narrow_min_width, narrow_min_height;   //? per-box minimums for the stacked layout
 	extern bool shown, redraw;
 	extern int select_max;
 	extern atomic<int> detailed_pid;
--- a/src/btop_tools.cpp
+++ b/src/btop_tools.cpp
@@ -116,6 +116,31 @@ namespace Term {
 		return false;
 	}
 
+	//* Width the side-by-side layout needs: mem/net beside proc, never narrower than the cpu box
+	static int wide_min_width(bool cpu, bool mem, bool net, bool proc) {
+		int width = 0;
+		if (mem) width = Mem::min_width;
+		else if (net) width = Mem::min_width;
+		width += (proc ? Proc::min_width : 0);
+		if (cpu and width < Cpu::min_width) width = Cpu::min_width;
+		return width;
+	}
+
+	auto narrow_layout(const string& boxes) -> bool {
+		bool cpu = boxes.find("cpu") != string::npos;
+		bool mem = boxes.find("mem") != string::npos;
+		bool net = boxes.find("net") != string::npos;
+		bool proc = boxes.find("proc") != string::npos;
+	#ifdef GPU_SUPPORT
+		//? The stacked layout knows nothing about gpu boxes: with one shown, the wide layout rules
+		if (Gpu::count > 0)
+			for (char i = '0'; i <= '5'; i++)
+				if (boxes.contains("gpu"s + i)) return false;
+	#endif
+		if (not (cpu or mem or net or proc)) return false;
+		return Term::width < wide_min_width(cpu, mem, net, proc);
+	}
+
 	auto get_min_size(const string& boxes) -> array<int, 2> {
         bool cpu = boxes.find("cpu") != string::npos;
         bool mem = boxes.find("mem") != string::npos;
@@ -127,11 +152,18 @@ namespace Term {
         	for (char i = '0'; i <= '5'; i++)
         		gpu += (boxes.contains("gpu"s + i) ? 1 : 0);
 	#endif
-        int width = 0;
-		if (mem) width = Mem::min_width;
-		else if (net) width = Mem::min_width;
-		width += (proc ? Proc::min_width : 0);
-		if (cpu and width < Cpu::min_width) width = Cpu::min_width;
+		//? Too narrow for the side-by-side layout: the boxes stack, each at full width, so the
+		//? terminal needs the widest box and the sum of the (shorter) stacked minimum heights
+		if (narrow_layout(boxes)) {
+			int width = 0, height = 0;
+			if (cpu) { width = max(width, Cpu::narrow_min_width); height += Cpu::narrow_min_height; }
+			if (mem) { width = max(width, Mem::narrow_min_width); height += Mem::narrow_min_height; }
+			if (net) { width = max(width, Net::narrow_min_width); height += Net::narrow_min_height; }
+			if (proc) { width = max(width, Proc::narrow_min_width); height += Proc::narrow_min_height; }
+			return { width, height };
+		}
+
+        int width = wide_min_width(cpu, mem, net, proc);
 	#ifdef GPU_SUPPORT
 		if (gpu != 0 and width < Gpu::min_width) width = Gpu::min_width;
 	#endif
--- a/src/btop_tools.hpp
+++ b/src/btop_tools.hpp
@@ -146,6 +146,10 @@ namespace Term {
 	//* Returns true if terminal has been resized and updates width and height
 	bool refresh(bool only_check=false);
 
+	//* True when the terminal is too narrow for the side-by-side layout of the given boxes, so they
+	//* stack at full width instead (a function of the terminal width and the boxes only)
+	auto narrow_layout(const string& boxes) -> bool;
+
 	//* Returns an array with the lowest possible width, height with current box config
 	auto get_min_size(const string& boxes) -> array<int, 2>;
 
