# The tests, on every push and pull request to main: the engine suite (scripts/test.sh, under
# every POSIX shell the runner has, with shellcheck) and the store UI crate's cargo tests.
name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  engine:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq shellcheck minisign dash busybox
      - name: scripts/test.sh
        run: bash scripts/test.sh

  ui:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Rust
        run: rustup update stable
      - name: cargo test
        run: cargo test --locked
